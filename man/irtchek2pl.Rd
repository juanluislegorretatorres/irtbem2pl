% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/irtchek2pl.R
\name{irtchek2pl}
\alias{irtchek2pl}
\title{Checking user speciflied input variables}
\usage{
irtchek2pl(
  Model = "2PL",
  data,
  PriorA = c(0, 0.25),
  PriorB = c(0, 4),
  InitialA = 1,
  InitialB = 0,
  Tol = 0.01,
  max.ECycle = 50L,
  max.MCycle = 10L,
  n.decimal = 5L,
  n.Quadpts = 50L,
  Theta.lim = c(-4, 4),
  Missing = -9,
  ParConstraint = FALSE,
  BiasSE = FALSE,
  D = 1
)
}
\arguments{
\item{Model}{A character string declaring the type of item model (e.g., "2PL").}

\item{data}{A matrix or data.frame consisting of dichotomous data
(1 for correct and 0 for wrong response), with missing data coded
as in \code{Missing}.}

\item{PriorA}{A numeric vector or matrix specifying the log-normal prior
distribution hyperparameters (mean and variance) for item discrimination.
Defaults to c(0, 0.25).}

\item{PriorB}{A numeric vector or matrix specifying the normal prior
distribution hyperparameters (mean and variance) for item difficulty.
Defaults to c(0, 4).}

\item{InitialA}{A single numeric value or vector specifying initial
values for item discrimination parameters. Defaults to 1.}

\item{InitialB}{A single numeric value or vector specifying initial
values for item difficulty parameters. Defaults to 0.}

\item{Tol}{A single numeric value specifying the convergence threshold
for E-step cycles. Defaults to 0.01.}

\item{max.ECycle}{A single integer specifying the maximum number of
E-step cycles. Defaults to 50L.}

\item{max.MCycle}{A single integer specifying the maximum number of
M-step cycles. Defaults to 10L.}

\item{n.decimal}{A single integer specifying the number of decimal places
for output formatting. Defaults to 5L.}

\item{n.Quadpts}{A single integer specifying the number of quadrature
points per dimension. Defaults to 50L.}

\item{Theta.lim}{A numeric vector of length 2 defining the integration
grid boundaries. Defaults to c(-4, 4).}

\item{Missing}{A single numeric value indicating missing elements.
Defaults to -9. Cannot be 0 or 1.}

\item{ParConstraint}{A logical value indicating whether parameters are
constrained within a reasonable range. Defaults to FALSE.}

\item{BiasSE}{A logical value determining whether to estimate SEs
directly from the inverted Hessian matrix. Defaults to FALSE.}

\item{D}{A numeric scaling constant (1 or 1.702). Defaults to 1.}
}
\value{
A combined list containing formatted data arrays, sample sizes,
prior specifications, initial parameter values, and convergence controls.
}
\description{
Verifies whether the input variables provided by the
user are correct based on the 2PL model specifications. If acceptable,
the function formats and returns them as a combined list; otherwise,
it throws an error
}
\examples{
data(dat01)
library(irtbem2pl)
 Checking_2PL<-irtchek2pl(Model = "2PL", dat01, D = 1)

}
\references{
\emph{Item Response Theory Parameter Estimation Techniques}
Second Edition, Revised and Expanded Frank B. Baker University ofWisconsin
Madison, Wisconsin, U.S.A. Seock-Ho Kim The University ofGeorgia
Athens, Georgia, u.S.A.
}
\author{
Juan Luis Legorreta Torres \email{jlegorreta2002@yahoo.com.mx}
}
